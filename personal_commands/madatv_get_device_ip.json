{
  "ATV - Get device IP address": [
    {
      "TYPE": "jobType.PASSTHROUGH",
      "SYNTAX": "ifconfig wlan0 |grep 'inet addr' |cut -d ':' -f2 |cut -d ' ' -f1 && ifconfig eth0 |grep 'inet addr' |cut -d ':' -f2 |cut -d ' ' -f1"
    }
  ]
}
